/**
 * Acento de marca — **#1230e0** (RGB 18, 48, 224).
 *
 * Sync con `app/globals.css`: `--primary`, `--accent`, `--ring`, `--brand-accent-rgb`.
 */
export const SITE_BRAND_HEX = '#1230e0' as const;

/** Centro del haz WebGL — un poco más claro que el principal */
export const SITE_BRAND_AURORA_HEX = '#3d5af1' as const;

export const SITE_BRAND_RGB = [18, 48, 224] as const;
